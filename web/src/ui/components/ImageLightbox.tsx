"use client";
import { useEffect, useState } from "react";
export function ImageLightbox({ src, alt }: { src: string; alt: string }) { const [open,setOpen]=useState(false); useEffect(()=>{ if(!open)return; const onKey=(e:KeyboardEvent)=>e.key==="Escape"&&setOpen(false); document.addEventListener("keydown",onKey); document.body.style.overflow="hidden"; return()=>{document.removeEventListener("keydown",onKey);document.body.style.overflow=""}},[open]); return <><button type="button" className="evidence-image-button" onClick={()=>setOpen(true)} aria-label={`Ver ${alt}`}><img src={src} alt={alt} loading="lazy" /></button>{open&&<div className="image-lightbox" role="dialog" aria-modal="true" aria-label={alt} onClick={()=>setOpen(false)}><button type="button" className="image-lightbox-close" onClick={()=>setOpen(false)} aria-label="Cerrar">×</button><img src={src} alt={alt} onClick={e=>e.stopPropagation()} /></div>}</> }
